apiVersion: v1
kind: Pod
meta-data:
  name: voting-app-pod
  labels:
    name:voting-app-pod
    app: demo-voting-app
spec:
  containers:
    - name: voting-app
     image: eesprit/voting-app-vote
      - containerPort:80
