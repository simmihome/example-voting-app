piVersion: v1
kind: Pod
meta-data:
  name: postgres-pod
  labels:
    name:postgres-pod
    app: demo-voting-app
spec:
  containers:
    - name: postgres
     image: postgres
      - containerPort:5432